// Background service worker (MV3)
// Reserved for future features: context menus, alarms, auth flows, content script messaging

chrome.runtime.onInstalled.addListener(() => {
  // Placeholder: could set up context menus here
});
